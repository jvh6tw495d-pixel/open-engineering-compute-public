# OEC — Auditoria v2.5.3 (via Grok Build CLI)

## 1. O QUE ESTÁ FEITO

### Estado do repositório
- Branch: `codex/oec-v2.5` | HEAD `0493ee6` (`feat(mcp): add confidence routing specialists`)
- Versão: **2.5.2**
- Working tree: 1 modificado (`docs/development/graphify.md`); plano v2.5.3 + relatórios Hermes **untracked**
- `src/oec/mcp/`: só `server.py`, `discovery.py`, `__init__.py` — **sem `envelope.py`**

### Histórico v2.5.x (commitado)
- **v2.5.0** (`e2cc779`): consolidação v2.4 (Backend Registry + Verification Engine) + v2.5 (kernel/Math IR)
- **v2.5.1** (`f1c09c3`): 4 skills `timeseries.*` (AR/ACF/PACF/Levinson-Durbin), cobertura, routing AR no `agent.default`
- **v2.5.2** (`0493ee6` + `64af581`): hardening do router MCP:
  - `needs_clarification` / `needs_more_information` (`discovery.py`)
  - ranking ponderado de intents (`rank_domain_intents`)
  - especialistas `agent.control_dynamics` e `agent.finance_uncertainty`
  - `sys.path` p/ `agents/` fora do cwd (`server.py`)
  - `_has_execution_payload` (evita review com execution vazio)
  - `OptimizationSpecialist.run_skill` p/ retry `skill_id`+`inputs`
  - catch amplo de exceções com erro estruturado

### Superfície atual relevante (pré-2.5.3)
- **MCP:** 8 tools `agent.*` + 2 discovery + skills brutas (`server.py`)
- **7 especialistas** em `agents/` + `common.py`
- **Sementes da Wave 2 (já existem, mas não são o contrato):** `claimed_objective` / `claimed_solver_status` em `agent.default` e `agent.scientific_reviewer` — **não** há `claimed_answer` nem `host_output_diverged`
- **Benchmarks:** `scripts/hermes_supertest.py`, `multiagent_with_without_oec.py`, `ollama_agent_stress_test.py` ainda medem via scrape de JSON do host (`extract_json` ~L131)
- **CHANGELOG:** última release **[2.5.2]**; seção [Unreleased] vazia; nenhuma entrada 2.5.3
- **Debt:** D-CUR-20…25; **D-CUR-26 não existe** (previsto só no plano)

### Diagnóstico: Wave 1 **não começou** em código
Só existe o plano (untracked) + menções em `docs/development/graphify.md` ("Nothing has been implemented", "ready to execute Wave 1"). Zero commits, módulos ou testes de `authoritative_answer` / `envelope` / `normalize` / divergência.

---

## 2. O QUE FALTA PARA FECHAR A 2.5.3

### Wave 1 — contract and normalization (T1–T8) — 100% PENDENTE
| Task | Status | Evidência |
|------|--------|-----------|
| T1 | Dataclasses `AuthoritativeAnswer`, `ProblemClassification`, `MethodSummary` + `AUTHORITATIVE_ANSWER_SCHEMA_VERSION="1.0"` em `src/oec/mcp/envelope.py` | **Pendente** | `envelope.py` não existe |
| T2 | `extract_executions(payload)->list[dict]` (9 shapes) | **Pendente** | sem símbolo |
| T3 | `build_authoritative_answer(executions, specialist)` | **Pendente** | sem símbolo |
| T4 | `_route_decision(...)` a partir de `_router_target_for` (confiança/reason/score) | **Pendente** | só `_router_target_for` em `server.py:445` |
| T5 | `normalize(payload, *, tool_name, decision)->dict` (aditivo) | **Pendente** | sem `normalize` de envelope |
| T6 | Wire `normalize` em `call_tool` p/ `_AGENT_TOOL_SCHEMAS` (~L741) | **Pendente** | `call_tool` não importa envelope |
| T7 | Integration tests (routed≡direct; math `scalar_extrema_result`; clarif. sem authority; smoke 7 agents) | **Pendente** | sem testes `*envelope*`/`*authoritative*` |
| T8 | Seção em `docs/mcp/README.md` + D-CUR-26 em `technical-debt.md` | **Pendente** | README só documenta `needs_*`; debt para em D-CUR-25 |

### Wave 2 — fail-closed safeguards — 100% PENDENTE
- Campo `claimed_answer` em `agent.default`/`agent.scientific_reviewer`: **Pendente** (só `claimed_objective`/`claimed_solver_status`)
- Helpers de divergência (pós-`json.dumps`/`default=str`): **Pendente**
- Shape `host_output_diverged`: **Pendente**
- Comparadores scalar/bool/vector/agregados: **Pendente**
- Manter contagem de tools (`len(tools)`): N/A ainda

### Wave 3 — harness / benchmark — 100% PENDENTE
- `hermes_supertest.py`: ainda `extract_json` + score; não lê `authoritative_answer`
- `multiagent_with_without_oec.py`: score único; sem 3 vereditos (transport/OEC/host)
- `ollama_agent_stress_test.py`: sem alinhamento ao envelope

### Wave 4 — real-model validation — 100% PENDENTE
- Smoke weak (`granite4:7b`) + strong (Nemotron/omniroute) não rodados sob contrato 2.5.3

### Wave 5 — release closeout — 100% PENDENTE
- CHANGELOG/release note 2.5.3: **Pendente**
- Bump versão 2.5.2→2.5.3: **Pendente**
- Graphify rebuild: **Pendente**
- Debt residual de host-integration: **Pendente**

### Conclusão de auditoria
Implementação da Wave 1 **não começou** — só o plano completo em `docs/implementation/v2.5.3-authoritative-output-hardening-plan.md` (status: planning complete — ready to execute Wave 1). Baseline de código ainda é v2.5.2 @ `0493ee6`.

---

## 3. SUGESTÕES / PRÓXIMOS PASSOS

### Ordem recomendada
1. **Commitar o plano** (hoje untracked) p/ baseline auditável — sem misturar com código
2. **Wave 1**, na ordem do kickoff:
   1. `src/oec/mcp/envelope.py` — T1+T2+T3
   2. Unit tests de `extract_executions`/`build_authoritative_answer` (9 shapes) ANTES de tocar `call_tool`
   3. T4: `_route_decision` + thin wrapper `_router_target_for`
   4. T5–T6: `normalize` + único wire em `call_tool` p/ `_AGENT_TOOL_SCHEMAS`
   5. T7 (integration) → suite completa sem editar asserts `payload["result"]`
   6. T8: `docs/mcp/README.md` + D-CUR-26
3. **Wave 2 em paralelo parcial com Wave 3** (Wave 3 vereditos transport+OEC pode começar quando Wave 1 existir; host corruption/claimed_answer bloqueiam só na Wave 2)
4. **Wave 4** só após 1+2 (e harness 3 p/ ler o envelope)
5. **Wave 5** por último: versão, CHANGELOG, Graphify, debt residual

### Riscos específicos
- `MathRequestReport` sem `execution` → math free-text sem authority → special-case `min_execution`/`max_execution`; T7(b)
- `ReviewReport` sem execution → decisão explícita `kind:"review_result"` via `passed`+`checks[]`
- Wrap em `_run_specialist_by_name` → recursão double-wrap → só wrap em `call_tool`
- Mutar `status` global → colide com `ExecutionStatus` no raw skill → só agent tools
- Novo top-level `result` → quebra ~32 asserts → espelhar keys aditivas
- Novo MCP tool na Wave 2 → quebra `len(tools)` → só campo `claimed_answer`
- Comparar pré- vs pós-serialização → `default=str` em `_json_text` → divergência em dicts serializados
- Working tree sujo → separar commits doc vs código; não commitar `.stress-tmp/`/stderr logs

### Critério de "Wave 1 fechada"
- `src/oec/mcp/envelope.py` commitado
- routed vs direct optimization com `authoritative_answer` IDÊNTICO
- math free-text com `kind == "scalar_extrema_result"`
- `needs_clarification`/`needs_more_information` SEM `authoritative_answer`
- suite existente verde sem reescrever nesting de `result`
