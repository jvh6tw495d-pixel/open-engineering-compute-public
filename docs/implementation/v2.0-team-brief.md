# Team brief — v2.0 Scientific Kernel (leader: Grok)

**Date:** 2026-07-27
**Baseline:** OEC **v1.5.0** private alpha
**Goal:** Ship **v2.0.0-alpha** Scientific Kernel *additive* layer (no breaking ExecutionResult).

## Roles

| Agent | Tool / model | Work package | Output paths |
|---|---|---|---|
| **Grok (lead)** | this session | K1–K3 core code + integration + gates | `src/oec/core/*`, `tests/unit/test_core_*`, wire `__init__` |
| **GLM 5.2** | OpenCode `opencode-go/glm-5.2` | K7 ADR 0019 | `docs/architecture/adr/0019-scientific-kernel.md` |
| **Sonnet** | Claude CLI `sonnet` | K6 test design review + any gaps | feedback; optional test patches |
| **GPT 5.6** | OpenCode `openai/gpt-5.6` | **Validation only** after merge | written review in `docs/implementation/v2.0-gpt56-validation.md` |

## Hard rules

1. **Do not break** `ExecutionResult` public shape.
2. Core must **not** import domain skills (`electrical`, `finance`, …).
3. Adapter: `ExecutionResult → ScientificResult` only.
4. No forbidden private names.
5. Commit small; lead integrates.

## Acceptance (v2.0-alpha slice)

- [ ] `from oec.core import ScientificResult, from_execution_result`
- [ ] Unit tests pass without domain packages
- [ ] ADR 0019 accepted
- [ ] GPT 5.6 validation recorded
- [ ] Version remains 1.5.0 until lead cuts `2.0.0` (or `2.0.0a1` if agreed)
