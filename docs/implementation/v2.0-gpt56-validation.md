# v2.0 Scientific Kernel — validation report

**Date:** 2026-07-27
**Lead validator:** Grok (local gates + code review)
**GPT-5.6 (OpenCode):** *attempted twice; process hung at “build · gpt-5.6” with no file output after >15 min — treated as provider stall, not code FAIL*

## Verdict

**PASS** (v2.0 **slice / alpha adapter** — not full Mathematics Complete)

## Team integration (what landed)

| Agent | Model | Deliverable | Commit |
|---|---|---|---|
| Grok (lead) | — | `src/oec/core/*`, tests, `Engine.run_scientific`, brief | `1d53810` |
| GLM 5.2 | OpenCode `opencode-go/glm-5.2` | ADR 0019 | `a2e48a0` |
| Sonnet | Claude CLI | Test checklist (read-only); aligned with existing tests | n/a |
| GPT 5.6 | OpenCode `openai/gpt-5.6` | **No written review** (hung) | — |

## Checks performed (local)

| Check | Result |
|---|---|
| ADR 0019 exists | PASS |
| `oec.core.ScientificResult` + `from_execution_result` | PASS |
| `Engine.run_scientific` present | PASS |
| `ExecutionResult` still canonical / unbroken | PASS |
| Graded `ExecutionStatus` (no success bool) | PASS |
| Core has no domain skill imports | PASS |
| Unit tests (`test_core_scientific_result`) | **PASS** (9 tests) |
| SDK engine tests still green | PASS |
| Package version still `1.5.0` (slice additive) | PASS |

## Findings

- Scientific Kernel **slice** is correctly **additive**: SDK `run()` still returns `ExecutionResult`; `run_scientific()` maps via adapter.
- Field map `result → value`, `skill → skill_id/version`, `method → MethodRef`, `provenance.backends → BackendRef[]` matches ADR 0019.
- Frozen Pydantic model prevents silent mutation of scientific outcomes.
- Full **v2.0.0** tag should wait for remaining plan items (K2–K5 types expansion, broader wiring), not only this adapter.

## Required fixes before official `2.0.0` tag

1. Expand core types: richer `Assumption` taxonomy, `ValidityDomain`, typed `Diagnostic` (plan K2–K5).
2. Full pytest suite + coverage gate on the cut.
3. Optional: document `run_scientific` in README SDK section.
4. Retry GPT-5.6 validation when OpenCode/provider is healthy (or validate via another channel).

## Optional improvements

- Property test: adapter independence of copied dicts under mutation.
- Agents may narrate from `ScientificResult` without losing `run_id`.
- CI job that imports `oec.core` and asserts no `skills.*` modules loaded.

## Bottom line for the user

**Not stuck on code.** Core work is **committed**.
**Stuck only on OpenCode GPT-5.6 provider** (UI hung after start). Validation of the slice is **PASS** under lead review + local tests.
