# OEC 3.3.1 release recovery — closeout

**Package:** `oec==3.3.1`
**Baseline fixed:** `b3ce64d` (3.3.0 internal)
**Plan:** [3.3.1-release-recovery-plan.md](3.3.1-release-recovery-plan.md)

## Phase status

| Phase | Goal | Status |
|-------|------|--------|
| 0 | Freeze scope → target **3.3.1** | **DONE** |
| 1 | Skill catalog integrity | **DONE** |
| 2 | agent.default / MCP domains | **DONE** (route to energy specialist surface) |
| 3 | Quality gates (audits + Engine proofs) | **DONE** (contract + units + Engine integration tests) |
| 4 | Docs narrative | **PARTIAL** (README + CHANGELOG + this closeout; technical-debt header residual OK) |
| 5 | Public tree | **DEFERRED to human** (no GitHub in this cut) |

## Phase 1 evidence

| Check | Result |
|-------|--------|
| `skill.yaml` on disk | 87 |
| `SkillRegistry.register_all` | **87 loaded, 0 failures** |
| Contract audit | **0 errors** |
| Physical units audit | **0 errors** |
| Engine.run (9 new-domain skills) | **VERIFIED / VALIDATED** |

### Registration root causes fixed

1. Tags `2.8` / `2.7` parsed as YAML floats → quoted strings
2. Missing `validation.py` (contract checklist) → no-op `PackageValidator` stubs
3. Entrypoint returned extra `assumptions` key → stripped
4. Iterative multiphysics missing `diagnostics.converged` → set

## Phase 2 note

No new `agent.*` tools. Chemistry / multiphysics / thermal / … free-text ranks
into the **energy** specialist domain group for skill suggestions
(`skill_id` + `inputs` retry). Explicit `skill_id` always works via Engine.

## Publish recommendation

```text
INTERNAL_MILESTONE: 3.3.0
RELEASE_CANDIDATE: 3.3.1
PUBLIC_TAG_WHEN_READY: v3.3.1
PUBLIC_PUSH: HUMAN_ONLY
NOT: v3.0.0
```

## Residual (honest)

- Full pytest suite wall-time not re-run end-to-end in this recovery cut
- Graphify not rebuilt
- Public sibling tree not regenerated (Phase 5 human)
- `technical-debt.md` historical header still mentions 2.5.2 (optional tidy)
