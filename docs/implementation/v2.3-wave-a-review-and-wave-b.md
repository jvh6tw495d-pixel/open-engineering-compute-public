# v2.3 Wave A review + Wave B delivery

**Date:** 2026-07-27  
**Branch:** `codex/oec-v2.5`  
**Package:** `oec==2.3.0` (Waves A+B+C closed)

## 1. Wave A review (post-fix)

| Check | Result |
|---|---|
| Skills | 11 experimental (linear×3, stats×3, TS×3, opt×2) |
| Contracts audit | clean (full catalog) |
| Unit + skill golden/validation | green after `steps_ahead` rename |
| Forbidden names | 0 (forecast lead field renamed to `steps_ahead`) |
| Version honesty | pre-release `2.3.0a0` (not full 2.3) |

### Residual (accepted, not blockers)

- No `test_properties.py` for Wave A skills  
- No coefficient standard errors on `statistics.regression`  
- Opt skills need `highspy` extra  

### Verdict

**Wave A PASS** as alpha slice. Not full v2.3 until Wave B/C as planned.

## 2. Wave B delivered

| Area | Skills |
|---|---|
| Uncertainty | `uncertainty.lhs`, `uncertainty.morris`, `uncertainty.propagate_linear` |
| Dynamics | `dynamics.state_space_simulate`, `dynamics.stability_margins` |
| Control | `control.pid_discrete`, `control.kalman_filter` |

**Scope notes**

- Morris is **linear-model only** (sandbox-safe; no arbitrary callables).  
- Propagation is first-order (delta method).  
- Continuous state-space uses SciPy `expm` ZOH; discrete is pure NumPy.  
- Sobol estimators deferred (noted in CHANGELOG).

## 3. Gate vs V3 §7

| Requirement | Status |
|---|---|
| ≥ 15 new applied skills | **18** (11 A + 7 B) |
| schema + golden + references + backend | yes for all Wave A/B |
| Wave C (Pareto / CVaR / robust LP) | **not** in this cut |

## 4. Wave C (delivered)

| Skill | Method |
|---|---|
| `optimization.pareto_lp` | weighted-sum Pareto sweep |
| `optimization.cvar_lp` | Rockafellar–Uryasev linear CVaR |
| `optimization.robust_lp` | box RHS worst-case robust LP |

## 5. Next

- v2.4 Verification Engine / backend registry  
- Independent multi-model validation of 2.3.0 if desired  

