# OEC v3.1 — release closeout (publishable)

**Date:** 2026-08-06
**Package:** `oec==3.1.1`
**Branch:** `feat/v2.6.1-wave-2` (incubation)
**Commits:** `d1b45de` (3.1.0) + `b500665` (3.1.1)

## 1. What 3.1 is

Feature line on top of multiphysics (2.7) and the earlier platform cut:

| Slice | Content |
|-------|---------|
| **3.1.0** | `oec.chemistry` (C1–C4 + Fick), Scientific IR v0, Model Registry + fidelity |
| **3.1.1** | Option A docs, chemistry skills + goldens, ΔG°→K, GOVERNANCE, migrations, RC/public checklists |

**Not included in 3.1:** public remote push (you do that), strong coupling, full Gibbs minimiser, coverage≥85% hard gate.

## 2. Gates (local)

| Gate | Result |
|------|--------|
| Version `3.1.1` | DONE |
| Chemistry unit + skill goldens | DONE |
| Coupling / registry unit | DONE (prior) |
| Forbidden brands on public tree | run at prepare time |
| Incubation remotes | empty (keep private) |
| Public push | **HUMAN** |

## 3. Public tree (prepared for you)

Sibling path (regenerated at close):

```text
../open-engineering-compute-public-3.1.1
```

Clean git history, **no remote**. You add remote + push when ready.

## 4. How you publish (only you)

```bash
# 1) Review
cd ../open-engineering-compute-public-3.1.1
# spot-check README, LICENSE, GOVERNANCE, src/oec, skills/chemistry

# 2) Optional re-gate
python scripts/check_forbidden_names.py --all-files

# 3) When authorized — NEW remote on PUBLIC tree only
git remote add origin git@github.com:<org>/open-engineering-compute.git
git push -u origin main
git tag v3.1.1
git push origin v3.1.1

# NEVER push the incubation repo history if it has private material
```

Suggested public tag: **`v3.1.1`** (matches package).
If you prefer a marketing “first public” story as `v3.0.0` on the clean tree, that is a human product choice — code version remains 3.1.1 in `pyproject.toml`.

## 5. Terminal

```text
OEC_V3_1: CLOSED_LOCAL_PUBLISHABLE
PACKAGE: oec==3.1.1
PUBLIC_TREE: open-engineering-compute-public-3.1.1
PUBLIC_PUSH: HUMAN_ONLY
INCUBATION_REMOTE: NONE
```
