# OEC 3.4.1 closeout — Evolutionary Neural Training (industrial)

**Date:** 2026-08-10
**Package:** `oec==3.4.1`
**Git tag:** `v3.4.1-neural-evo`
**ADRs:** 0031, 0032, **0033**
**Theme:** Gradient ∪ Evolutionary ∪ Hybrid training; product-agnostic OEC

## Decision

**GO** to declare the evolutionary neural training requirement **closed at
industrial incubation level**:

- Three modes implemented as skills + kernel.
- Hybrid evo→gradient is the strategic default for non-toy models.
- Native pymoo multi-obj neural search (rmse, n_params) + multi-seed hybrid outer.
- Strategy benchmark (gradient / hybrid / neuroevolution) under shared budget.
- No proprietary inference-engine product branding in OEC docs.
- Version **3.4.1** tagged as `v3.4.1-neural-evo` (local).

## Catalog

| Metric | Value |
|--------|-------|
| Skills (contract audit) | **131** clean |
| Neural training/search skills (ADR 0033) | **10** new |
| Domains | **22** (neural count increased) |

### ADR 0033 skills

| Skill | Role |
|-------|------|
| `neural.training.supervised` | Gradient supervised entry |
| `neural.training.gradient` | Explicit gradient path |
| `neural.training.hybrid` | Evo outer → torch inner (+ multi-seed, multi-obj flags) |
| `neural.training.neuroevolution` | Small-MLP weight evo (max_params fail-closed) |
| `neural.search.hyperparameters` | HPO facet |
| `neural.search.architecture` | Arch search (pymoo NSGA-II multi-obj) |
| `neural.search.features` | Feature-mask evolution |
| `neural.search.loss_weights` | λ composition search |
| `neural.search.policy` | Train-policy catalog evolution |
| `neural.benchmark.training_strategy` | Three-arm controlled comparison |

## Industrial gates (tests)

| Gate | Coverage |
|------|----------|
| Budget + inner_training always present | `test_budget_fields_always_present` |
| Fail-closed large neuroevolution | `test_fail_closed_neuroevolution_large` |
| Multi-seed outer mean±std | `test_multiseed_outer_mean_std` |
| pymoo multi-obj Pareto | `test_pymoo_multiobjective_pareto` |
| Benchmark three arms | `test_benchmark_three_arms_complete` |

## Explicit non-goals (remain)

- RL policy evolution as first-class product
- Neuroevolution of large models (use hybrid)
- Proprietary product names/datasets in the OEC tree
- Promoting skills to `validated` (still `experimental`)
- Public GitHub push (local closeout only unless requested)

## Residual optional polish

| Item | Severity |
|------|----------|
| DEAP structural genotype (vs Nevergrad weights) | Low |
| Latency/memory as third multi-obj axis | Low |
| Skill status `validated` after long multi-seed campaigns | Medium product process |
| PyPI / remote push of tag | Process / release (local tag only unless requested) |

## Version claim

```text
pyproject.toml / oec.__version__ = 3.4.1
git tag = v3.4.1-neural-evo
skills total = 131 (contract-clean)
ADR 0033 W1–W6 core + industrial multi-seed/multi-obj = delivered
product inference-engine branding = not in OEC tree
physics time-horizon terms = retained
```
