# OEC v2.3 Wave A/B correction report

**Date:** 2026-07-27  
**Branch:** `codex/oec-v2.5`  
**Package:** `oec==2.3.0` (corrections applied; re-validation)  

## Summary of corrections

| ID | Fix |
|---|---|
| **A23-01** | `population_standard_deviation` replaces `known_variance`; skill `statistics.intervals` **0.2.0**; `dispersion_used` on output |
| **A23-02** | Drop-one heuristic honest naming (`single_constraint_relaxations`, `claims_iis=false`); no unproven IIS claims; tiers for budget/unknown/time_limit; skill **0.2.0** |
| **B23-01** | Kalman: PSD/PD checks, Joseph update, `solve` not `inv`, structured `KalmanNumericalError`; skill **0.2.0** |
| **B23-02** | Propagate: `nominal` length/finite checks + `nominal_output` |
| **B23-03** | Morris: even `n_levels>=4`; method `morris_linear_screen`; effect units note |
| **B23-04** | Spectral classification stable/marginal/unstable; `margin_kind` documents not gain/phase |
| **B23-05** | PID: `anti_windup` none/clamp, `integral_term` |
| **A23-03** | LP diagnostics docs: no MIP gap claims (MILP owns gap) |
| **Lint** | Removed one-shot scaffolder scripts |
| **Coverage** | Adversarial + boost tests → **≥ 90%** |

## Gates (candidate)

```text
uv run ruff check .          → PASS
uv run mypy src              → PASS
uv run pytest -q             → 1154+ passed, coverage ≥ 90%
uv run python scripts/audit_skill_contracts.py → 62/62
uv run python scripts/audit_physical_units.py  → 0 errors
uv run python scripts/check_forbidden_names.py → 0
```

## Residual limitations (accepted)

- Infeasibility skill remains a **drop-one heuristic**, not a certified IIS.
- Morris is **linear-model only**.
- Kalman R must be positive definite (semidefinite R not supported).
- Spectral stability ≠ classical gain/phase margins.

## Follow-up closeout (schema/versions)

| Skill | Version |
|---|---|
| `statistics.intervals` | 0.2.0 |
| `optimization.infeasibility_explain` | 0.2.0 |
| `control.kalman_filter` | 0.2.0 |
| `uncertainty.propagate_linear` | **0.2.0** (`nominal_output` in schema + schema-runtime tests) |
| `control.pid_discrete` | **0.2.0** |
| `uncertainty.morris` | **0.2.0** |
| `dynamics.stability_margins` | **0.2.0** |

## Decision

**GO técnico for 2.3.0** after schema/`nominal_output` closeout + green gates
(subject only to independent reviewer sign-off if still required by process).
