# OEC 3.4.0 closeout

**Date:** 2026-08-02
**Package:** `oec==3.4.0`
**Theme:** Neural Compute + Evolutionary Compute (optional extras)
**ADRs:** 0031 (neural/evo governance), 0032 (GNN pure torch)

## Decision

**GO to declare 3.4.0** as the incubation release that folds the complete
Neural/Evolutionary wave program (N0–N5, E1–E4, X1–X3) into the tree, without
changing core required dependencies.

## Scope delivered

| Area | Status |
|------|--------|
| Optional extras `neural` / `evolutionary` | Done |
| Backend probes + fail-closed method map | Done |
| Declarative contracts (no agent Python inject) | Done |
| 34 new skills (neural 16 + evo 15 + hybrid 2 + scientific 1) | Done |
| Catalog 87 → **121** skills, 18 → **22** domains | Done |
| Skill contract audit | 121/121 clean |
| Contract fixtures for whole-catalog integration test | Done (extras skipped if missing) |
| CHANGELOG / README / skill-inventory | Done |
| Local tag `v3.4.0` | Closeout step |

## Explicit non-goals of 3.4.0

- Public GitHub launch (still later / public-alpha procedure)
- Promoting neural/evo skills to `stable` / `validated`
- PyG/DGL as hard deps (ADR 0032)
- Treating surrogate optima as physics truth (X2 policy)
- Full CI job matrix with torch/pymoo on every PR (markers deselect by default)

## Residual risks / follow-ups

| Item | Severity | Action |
|------|----------|--------|
| Default pytest skips `@neural` / `@evolutionary` | Medium | Optional CI job `extras-neural-evolutionary` (nightly / dispatch); strong goldens in `tests/golden/test_neural_evolutionary_strong.py` |
| Predict/evaluate fixtures embed a tiny trained checkpoint | Low | Regenerate if torch serialization changes |
| Branch `feat/v2.6.1-wave-2` may hold unrelated dirty files | Medium | Keep 3.4 commit scoped; clean or split branch later |
| `uv.lock` may need regen after extras | Low | `uv lock` on release machine |
| Forbidden-names hits in old Hermes scripts | Pre-existing | Out of 3.4 scope |
| Agent narrative authority | Low | P0: `agents.common` + `scripts/check_agent_no_number_without_run_id.py` |

## Gate checklist (run at tag)

```text
uv run ruff check src/oec
uv run mypy src
uv run pytest -q                                    # core (extras deselected)
uv run python scripts/audit_skill_contracts.py --no-write-report
# optional full extras:
# uv sync --extra neural --extra evolutionary
# uv run pytest -m "neural or evolutionary" --import-mode=importlib -o addopts=
```

## Version claim

```text
pyproject.toml / oec.__version__ = 3.4.0
skills total = 121
domains = 22
```
