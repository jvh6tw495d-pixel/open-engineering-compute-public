# OEC v3.0.0 — Public platform claim (local closeout)

**Date:** 2026-08-06
**Package:** `oec==3.0.0`
**Branch:** `feat/v2.6.1-wave-2` (incubation)
**EOC:** Grok Build

> **v3.0 meaning (explicit):** first **public-claimable** cut of the Open Engineering
> Compute platform (Option **C** of `OEC_V3_IMPLEMENTATION_PLAN.md` § risks —
> go-to-market platform launch).
> **Not claimed:** full V3 “Math+Physics+Chemistry unificados” (years of work).
> Chemistry / Model Registry were deferred here; delivered as foundation v0 in
> **`oec==3.1.0`** (closeouts `v2.8-CLOSEOUT.md`, `v2.9-CLOSEOUT.md`).
> **Public GitHub push:** still a **human** step (ADR 0008).

---

## 1. Scope claim

| In 3.0.0 | Deferred |
|----------|----------|
| Skill engine + validation + sandbox | Chemistry complete (was 2.8) |
| Scientific Kernel (`oec.core`) | Full Scientific IR / Model Registry (was 2.9) |
| Math / opt / TS / stats / linear / … skills | Strong multiphysics |
| Physics foundation P1–P5 + energy | Public remote + tag on GitHub.com |
| Multiphysics weak co-sim (`oec.physics.coupling`, from 2.7) | Incubation history publish |
| SDK / CLI / REST / MCP | — |
| Agents (out of wheel) | — |
| Public tree prep (sibling, new history) | — |

---

## 2. Gates (this cut)

| Gate | Result |
|------|--------|
| Version bump `3.0.0` | **DONE** (`pyproject.toml`, `src/oec/__init__.py`) |
| Coupling tests | **9 passed** (2.7) |
| Physics unit slice | **27 passed** (physics + service metrics) |
| Forbidden-names (product brands) | **case-sensitive brands** (English “horizon” allowed) |
| `prepare_public_alpha` | excludes `docs/implementation` + incubation host harnesses |
| Public tree forbidden gate | **must pass** on sibling dest before claim |
| `uv build` / full pytest | optional wall-time residual |
| Public remote push | **NOT DONE** (by design) |

### Public-tree exclusions (ADR 0008)

- `docs/implementation/*` — incubation plans, stress reports, agent handoffs
- Host/integration harness scripts with private paths or product brands
  (`hermes_supertest.py`, `direct_model_supertest.py`, `wave3_energy_smoke.py`, …)
- Master handbooks, `graphify-out`, venvs, caches

### Sanitization notes

- Product brands in public docs/MCP guide rewritten to generic “host agent” / “MCP host”.
- Incubation tree may still contain brand names under `docs/implementation` and
  excluded scripts — those paths are **not** copied into the public sibling.

---

## 3. Publish procedure (human only)

```bash
# incubation (already run for local claim)
python scripts/prepare_public_alpha.py \
  --dest ../open-engineering-compute-public-3.0.0 \
  --init-git

# in public tree: review, then ONLY if João authorizes:
# git remote add origin git@github.com:<org>/open-engineering-compute.git
# git push -u origin main
# git tag v3.0.0 && git push origin v3.0.0
```

**Do not** push incubation history. Public remote is empty by design until human gate.

---

## 4. Terminal

```text
OEC_V3_0_0: CLAIMABLE_LOCAL_PLATFORM
PUBLIC_PUSH: HUMAN_GATE
CHEMISTRY: DEFERRED_3_1
MULTIPHYSICS_WEAK: FROM_2_7
BASELINE: oec==3.0.0
```
