{
  "type": "object",
  "properties": {
    "x": {
      "type": "array",
      "minItems": 4,
      "items": { "type": "array", "minItems": 1, "items": { "type": "number" } }
    },
    "y": { "type": "array", "minItems": 4, "items": { "type": "number" } },
    "budget": { "type": "integer", "minimum": 3, "maximum": 50, "default": 8 },
    "epochs": { "type": "integer", "minimum": 5, "default": 20 },
    "seed": { "type": "integer", "default": 42 },
    "device": { "type": "string", "enum": ["cpu", "cuda", "auto"], "default": "cpu" },
    "task": { "type": "string", "enum": ["regression"], "default": "regression" }
  },
  "required": ["x", "y"],
  "additionalProperties": false
}
