id: neural.mlp.regressor
version: 0.1.0
status: experimental
domain: neural
title: MLP Regressor (PyTorch)

entrypoint:
  module: implementation
  function: execute

schemas:
  input: input.schema.json
  output: output.schema.json

method:
  id: torch_mlp_regressor_train
  version: 0.1.0
  iterative: true

execution:
  deterministic: false
  timeout_seconds: 300
  network_access: false
  filesystem_access: true

validation:
  schema: true
  dimensional: false
  mathematical: true
  physical: false
  numerical: true

references:
  - "PyTorch — https://pytorch.org/docs/stable/index.html"
  - "ADR 0031 Neural and Evolutionary Compute"

tags:
  - neural
  - mlp
  - experimental
