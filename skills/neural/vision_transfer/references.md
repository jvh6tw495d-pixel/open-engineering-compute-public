# neural.vision.transfer

Backends: torchvision ResNet18 (optional install) or Transformers CLIP
(`oec[foundation]`, pinned revision). OEC owns the MLP head, metrics, and
the comparison experiment (`frozen_features` vs `finetune_head`).
