{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "neural.mlp.classifier input",
  "type": "object",
  "properties": {
    "x": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "array",
        "minItems": 1,
        "items": { "type": "number" }
      }
    },
    "y": {
      "type": "array",
      "minItems": 2,
      "items": { "type": "number" },
      "description": "Integer class labels 0..C-1"
    },
    "n_classes": { "type": "integer", "minimum": 2, "default": 2 },
    "hidden_dims": {
      "type": "array",
      "items": { "type": "integer", "minimum": 1 },
      "default": [32, 16]
    },
    "activation": {
      "type": "string",
      "enum": ["relu", "gelu", "tanh", "sigmoid", "identity"],
      "default": "relu"
    },
    "epochs": { "type": "integer", "minimum": 1, "default": 80 },
    "batch_size": { "type": "integer", "minimum": 1, "default": 16 },
    "lr": { "type": "number", "exclusiveMinimum": 0, "default": 0.01 },
    "val_fraction": { "type": "number", "minimum": 0, "maximum": 0.9, "default": 0.2 },
    "seed": { "type": "integer", "default": 42 },
    "device": { "type": "string", "enum": ["cpu", "cuda", "auto"], "default": "cpu" }
  },
  "required": ["x", "y"],
  "additionalProperties": false
}
