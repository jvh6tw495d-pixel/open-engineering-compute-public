id: battery.soc_step
version: 0.2.1
status: experimental
domain: battery
title: Battery SOC Step (Energy-Based)

entrypoint:
  module: implementation
  function: execute

schemas:
  input: input.schema.json
  output: output.schema.json

method:
  id: energy_based_step
  version: 0.1.1
  iterative: false

execution:
  deterministic: true
  timeout_seconds: 30
  network_access: false
  filesystem_access: false

validation:
  schema: true
  dimensional: true
  mathematical: true
  physical: true
  numerical: true

references:
  - "Generic energy-based SOC integration (power × time × efficiency / capacity)"

tags:
  - mvp
  - battery
