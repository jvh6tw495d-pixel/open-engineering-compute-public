id: foundation.vllm_generate
version: 0.1.0
status: validated
domain: foundation
title: Remote vLLM Generation (OpenAI-compatible HTTP client)

entrypoint:
  module: implementation
  function: execute

schemas:
  input: input.schema.json
  output: output.schema.json

method:
  id: vllm_remote_completions
  version: 0.1.0
  iterative: true

execution:
  deterministic: false
  timeout_seconds: 30
  network_access: true
  filesystem_access: false

validation:
  schema: true
  dimensional: false
  mathematical: true
  physical: false
  numerical: true

references:
  - "vLLM remote backend — ADR 0046 (re-opens ADR 0040 D2, client only)"
  - "vLLM OpenAI-compatible server: https://docs.vllm.ai"

tags:
  - foundation
  - vllm
  - remote
