id: foundation.peft_train
version: 0.1.0
status: experimental
domain: foundation
title: PEFT / Full Fine-Tune Training (transformers + peft)

entrypoint:
  module: implementation
  function: execute

schemas:
  input: input.schema.json
  output: output.schema.json

method:
  id: transformers_peft_train
  version: 0.1.0
  iterative: true

execution:
  deterministic: false
  timeout_seconds: 600
  network_access: false
  filesystem_access: true

validation:
  schema: true
  dimensional: false
  mathematical: true
  physical: false
  numerical: true

references:
  - "S1 Scientific AI Completion — ADR 0040, ADR 0041"
  - "Hugging Face Transformers + PEFT"

tags:
  - s1
  - foundation
  - peft
