id: energy.grid_zero_feasibility
version: 0.1.0
status: experimental
domain: energy
title: Grid-Zero Feasibility (Provided Trajectory)

entrypoint:
  module: implementation
  function: execute

schemas:
  input: input.schema.json
  output: output.schema.json

method:
  id: grid_zero_trajectory_check
  version: 0.1.0
  iterative: false

execution:
  deterministic: true
  timeout_seconds: 30
  network_access: false
  filesystem_access: false

validation:
  schema: true
  dimensional: true
  mathematical: false
  physical: false
  numerical: true

references:
  - "OEC v2.6.1-EXECUTION-PLAN.md D4 — feasibility of a provided trajectory (no solver)."
  - "Distinct from energy.min_storage_capacity (optimization via optimization.lp)."

tags:
  - mvp
  - energy
  - grid-zero
  - feasibility
