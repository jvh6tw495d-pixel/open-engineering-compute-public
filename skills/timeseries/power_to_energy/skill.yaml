id: timeseries.power_to_energy
version: 0.1.0
status: experimental
domain: timeseries
title: Power to Energy

entrypoint:
  module: implementation
  function: execute

schemas:
  input: input.schema.json
  output: output.schema.json

method:
  id: trapezoidal_power_energy
  version: 0.1.0
  iterative: false

execution:
  deterministic: true
  timeout_seconds: 30
  network_access: false
  filesystem_access: false

validation:
  schema: true
  dimensional: false
  mathematical: true
  physical: false
  numerical: true

references:
  - "Trapezoidal integration of power over time"

tags:
  - mvp
  - timeseries
