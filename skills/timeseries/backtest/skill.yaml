id: timeseries.backtest
version: 0.1.0
status: experimental
domain: timeseries
title: Backtest (rolling, simple forecasters)

entrypoint:
  module: implementation
  function: execute

schemas:
  input: input.schema.json
  output: output.schema.json

method:
  id: rolling_simple_backtest
  version: 0.1.0
  iterative: true

execution:
  deterministic: true
  timeout_seconds: 60
  network_access: false
  filesystem_access: false

validation:
  schema: true
  dimensional: false
  mathematical: true
  physical: false
  numerical: true

references:
  - "Hyndman, R. J., Athanasopoulos, G. (2018). Forecasting: Principles and Practice, OTexts, Chapter 5 — backtest evaluation and the naive skill-score baseline."

tags:
  - mvp
  - timeseries
  - backtest
  - forecast
