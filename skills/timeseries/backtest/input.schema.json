{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "timeseries.backtest input",
  "type": "object",
  "properties": {
    "series": {
      "type": "array",
      "minItems": 2,
      "items": { "type": "number" }
    },
    "steps_ahead": { "type": "integer", "exclusiveMinimum": 0 },
    "method": {
      "type": "string",
      "enum": ["naive", "mean", "seasonal_naive"]
    },
    "period": { "type": ["null", "integer"], "exclusiveMinimum": 0 },
    "n_evaluations": { "type": ["null", "integer"], "exclusiveMinimum": 0 }
  },
  "required": ["series", "steps_ahead", "method"],
  "additionalProperties": false
}